"use client";
import * as React from "react";
import Loading from "@/components/loading";

const Home = () => {
    return (
        <React.Fragment>
            <Loading />
        </React.Fragment>
    );
};
export default Home;

